#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
OUT_DIR="$ROOT_DIR/dist"
OUT_FILE="$OUT_DIR/out_autok_sqlite.so"

mkdir -p "$OUT_DIR"

(
  cd "$ROOT_DIR"
  CGO_ENABLED=1 go build -buildmode=c-shared -o "$OUT_FILE" ./cmd/out_autok_sqlite
)

echo "$OUT_FILE"
